function createAction() {
  let actionType = action;
  let performer = performer;
  let target = target; // How do we best get the information here?
  let final = `${actionType}(${performer}, ${target})`;
}
